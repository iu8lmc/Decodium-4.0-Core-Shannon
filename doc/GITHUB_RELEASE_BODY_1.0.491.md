# Decodium 4 FT2 1.0.491

## Italiano

Punti principali (`1.0.490 -> 1.0.491`):

- **Nuova lingua: Olandese (Nederlands)** — 14ª lingua dell'interfaccia, completa (0 stringhe non tradotte),
  sia nell'applicazione sia nell'installer.
- **Disinstallazione corretta**: risolto l'errore "file in uso" che poteva far fallire la disinstallazione
  (e l'auto-disinstallazione durante gli aggiornamenti). Ora l'app viene chiusa in modo pulito dal Restart
  Manager di Windows — e forzatamente se e' bloccata — prima di rimuovere i file.
- Include i miglioramenti di stabilita' e della pipeline di decodifica della 1.0.490.

## English

Release highlights (`1.0.490 -> 1.0.491`):

- **New language: Dutch (Nederlands)** — the 14th interface language, fully translated (0 untranslated
  strings), in both the application and the installer.
- **Fixed uninstall**: resolved the "file in use" error that could make uninstallation fail (and the
  automatic uninstall during updates). The app is now closed cleanly by the Windows Restart Manager — and
  force-closed if it is frozen — before files are removed.
- Includes the stability and decode pipeline improvements from 1.0.490.
